import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerItem: {
      flexDirection: "row",
      padding: 10,
      margin: 20,
    },
    logo: {
      width: 50,
      height: 50,
    },
});

export default styles;